spring:
  application:
    name: crypto-exchange

server:
  port: ${SERVER_PORT:8400}

# Eksterni API sa kursevima kripto valuta - otvoren, ne zahteva API kljuc.
external:
  crypto-api:
    url: ${CRYPTO_API_URL:https://api.coinbase.com}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 10000

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.soas: INFO
